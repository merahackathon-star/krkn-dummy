name: krkn
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.11
  - go
  - hugo>=0.120.0  # Ensure we get a recent extended version if possible
  - pip
  - pip:
      - fastapi
      - uvicorn[standard]
      - httpx
      - pytest
      - pytest-asyncio
      - google-genai
      - gitpython
